<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/toMeasureButton"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_marginStart="104dp"
        android:layout_marginTop="292dp"
        android:background="#FFA500"
        android:onClick="toMeasure"
        android:text="Measure"
        android:visibility="visible"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/toStartCaptureButton"
        android:layout_width="200dp"
        android:layout_height="62dp"
        android:layout_marginTop="60dp"
        android:background="#FFA500"
        android:onClick="toStartCapture"
        android:text="Start Capture"
        app:layout_constraintStart_toStartOf="@+id/toMeasureButton"
        app:layout_constraintTop_toBottomOf="@+id/toMeasureButton" />

</androidx.constraintlayout.widget.ConstraintLayout>
